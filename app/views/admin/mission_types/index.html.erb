<h1>Listing mission_types</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Class name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @mission_types.each do |mission_type| %>
      <tr>
        <td><%= mission_type.name %></td>
        <td><%= mission_type.class_name %></td>
        <td><%= link_to 'Show', [:admin, mission_type] %></td>
        <td><%= link_to 'Edit', edit_admin_mission_type_path(mission_type) %></td>
        <td><%= link_to 'Destroy', [:admin, mission_type], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Mission type', new_admin_mission_type_path %>
