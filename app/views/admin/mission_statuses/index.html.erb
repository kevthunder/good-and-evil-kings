<h1>Listing mission_statuses</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Code</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @mission_statuses.each do |mission_status| %>
      <tr>
        <td><%= mission_status.name %></td>
        <td><%= mission_status.code %></td>
        <td><%= link_to 'Show', [:admin, mission_status] %></td>
        <td><%= link_to 'Edit', edit_admin_mission_status_path(mission_status) %></td>
        <td><%= link_to 'Destroy', [:admin, mission_status], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Mission status', new_admin_mission_status_path %>
