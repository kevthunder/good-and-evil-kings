<h1>Listing ai_actions</h1>

<table>
  <thead>
    <tr>
      <th>Type</th>
      <th>Weight</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @ai_actions.each do |ai_action| %>
      <tr>
        <td><%= ai_action.type %></td>
        <td><%= ai_action.weight %></td>
        <td><%= link_to 'Show', [:admin, ai_action] %></td>
        <td><%= link_to 'Edit', edit_admin_ai_action_path(ai_action) %></td>
        <td><%= link_to 'Destroy', [:admin, ai_action], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ai action', new_admin_ai_action_path %>
